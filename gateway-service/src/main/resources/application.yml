spring:
  cloud:
    gateway:
      routes:
      - id : wallet-service
        uri: http://localhost:8088
        predicates :
          - path=/wallet/**
      - id : operation-service
        uri: http://localhost:8089
        predicates:
          - path=/operation/**
  application:
    name: gateway-service
server:
  port: 8081